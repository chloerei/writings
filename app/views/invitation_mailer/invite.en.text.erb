<%= @space.user.display_name %> invite you to space <%= @space.display_name %>:

<%= @invitation.message %>

Writings.io is a website to write, manage and publish articles.

Accept this invitation to get started:

<%= dashboard_invitation_url(:space_id => @space, :id => @invitation.token, :host => APP_CONFIG['host'], :protocol => 'https') %>

<% if @space.members.any? %>
Other members on the workspace are:

<%= @space.members.map(&:display_name).join(', ') %>
<% end %>
