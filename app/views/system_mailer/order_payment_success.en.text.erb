Order <%= @order.id %> Payment Success.

Order Content: <%= I18n.t "plan.#{@order.plan}" %> <%= @order.quantity %> month(s).

Visit this link to show detail:

<%= dashboard_order_url(@order.space, @order, :host => APP_CONFIG['host'], :protocol => 'https') %>
