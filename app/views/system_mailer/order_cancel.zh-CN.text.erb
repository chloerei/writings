订单 <%= @order.id %> 被取消。

订单内容：<%= I18n.t "plan.#{@order.plan}" %> <%= @order.quantity %> 个月。

访问以下链接查看详情：

<%= dashboard_order_url(@order.space, @order, :host => APP_CONFIG['host'], :protocol => 'https') %>
