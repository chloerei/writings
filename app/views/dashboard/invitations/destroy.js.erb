$('#invitation-<%= @invitation.id %>').remove();

$('#invitation-form').replaceWith('<%= j render(:partial => 'dashboard/members/form') %>');

AlertMessage.show({ type: 'success', text: '<%= t('success') %>', timeout: 1500 });
