$('#articles-spinner').hide();
var $articles = $('#articles');
$articles.find('tbody').append('<%= j render(:partial => 'article', :collection => @articles) %>');

<% count = @articles.to_a.count %>
<% next_skip = @articles.options[:skip].to_i + count %>

$articles.data('skip', <%= next_skip %>);

<% if articles_is_end?(@articles) %>
  $articles.data('is-end', true);
  $('#end-message').show();
<% end %>
