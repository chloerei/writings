<% if logined? %>
  Turbolinks.visit('<%= j(session.delete(:return_to) || root_url) %>');
<% else %>
  AlertMessage.show({
    type: 'error',
    text: "<%= j @user.errors.full_messages.join %>"
  });
<% end %>
