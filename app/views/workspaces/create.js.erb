<% if @workspace.persisted? %>
  Turbolinks.visit('<%= dashboard_root_path(@workspace) %>')
<% else %>
  AlertMessage.show({ type: 'error', text: '<%= @workspace.errors.full_messages.join %>'})
<% end %>
