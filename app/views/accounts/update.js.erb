<% if current_user.errors.empty? %>
  Turbolinks.visit('<%= account_path %>');
<% else %>
  <% current_user.errors.full_messages.each do |message| %>
    AlertMessage.show({ type: 'error', text: "<%= j message %>" });
  <% end %>
<% end %>
