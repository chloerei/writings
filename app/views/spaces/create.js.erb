<% if @space.persisted? %>
  Turbolinks.visit('<%= dashboard_root_path(@space) %>')
<% else %>
  <% @space.errors.full_messages.each do |message| %>
    AlertMessage.show({ type: 'error', text: "<%= j message %>" });
  <% end %>
<% end %>
